{
    "environments": {
        "test": {
            "env": {
                "PYTHONUNBUFFERED": "true",
                "HEROKU_CI": "true",
                "RC_ROOT": "https://www.recurse.com"
            },
            "scripts": {
                "test-setup": "pip install -r requirements.txt && alembic upgrade head",
                "test": "python tests.py"
            },
            "addons": [
                "heroku-postgresql:hobby-dev"
            ]
        }
    }
}
